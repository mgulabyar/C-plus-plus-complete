#pragma once
#include <string>

// Struct bilkul Mongoose schema template ki tarah data structures map karta hai
struct Product {
    int id;
    std::string name;
    double price;
    int stock;
};
